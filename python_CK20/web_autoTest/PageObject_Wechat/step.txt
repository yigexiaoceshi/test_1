1、建模
2、初始用例（首页点击添加员工）测试用例进行初始化
3、测试用例（首页点击添加员工）具体步骤编写（元素定位需要非常熟练）
4、在添加成员的测试用例详细步骤编写时，实现driver的传递，可以在执行第一个方法（首页添加员工）的时候return里将driver传递给下一个页面实例
5、下一个页面实例（PageAddMember）调用当前页面方法的时候会进行实例化，但是在第一个方法的时候就已经实例化过，为了避免重复实例化，添加构造函数，将传递过来的driver实例赋值给当前的类变量，为了提高程序识别度，driver加个类型注解
6、继续第3，4，5点，如果跳转的页面较多，driver需要一致添加构造函数来进行传递，所以将构造函数抽离出来作为一个公共方法，建一个共同的父类（和业务无关），定义一个类的实例变量，才可以传入到子类的方法里去，子类页面进行继承（包括第一个方法里的driver初始化）
7、添加conf包，添加登录模块，获取cookie的方法，植入cookie的方法写在公共方法里
8、问题：所有子类每次实例化类对象的时候，都会调用构造函数，子类没有就会调用父类的构造函数，这样会造成每调用构造函数一次，就会开启一个新的浏览器窗口，所以在构造函数添加一个形参，用来接收形参有传入的时候来接收父类的实例对象，首次不传入参数就是None就走初始化分支
9、对第8点的理解，传入的形参是当前页面类的实例变量，所以不为空，而且可以用来接收父类的实例对象，尝试？？？传入一个a，用a接收父类的实例对象，然后赋值给当前子类的实例对象


三步优化：封装样板代码，常用UI操作封装在basepage；页面上复用率较高的元素定位抽离为私有变量（不要暴露页面元素暴露给外部）；起始页面的URL抽离
10、如果每次查找之后都需要添加及删除打印日志信息，则会很麻烦，公共方法里添加个find方法，传入定位方式和操作动作by，locator,返回查找方法by=by,value=locator
11、参数化，测试数据放在测试用例里，测试步骤里使用变量接收数据，PO不维护用例相关的内容，比如测试数据
12、多个方法用到相同的元素定位，定义为"私有"类变量时，传入定位方式和定位元素的值，这样影响到开始find方法里的传参，要做一个if判断，传入的可能是个元组（即封装好的类变量），也有可能没有定义为类变量时，传入的就是定位方法by和元素值locator，元组解包(*元组)
13、接第12点，一定个要是私有的类变量（_变量名 = ""），意思是当前类可以使用，可以通过继承的方法，在子类里赋予类属性(self._变量名)就可以在子类里进行调用
14、每条用例的起始URL不同，这是跟业务相关的，所以在父类和所有涉及到用例起始页面的子类都定义一个相同的"私有变量"初始URL，子类的的初始URL有值时，不会调用父类的私有变量
15、

